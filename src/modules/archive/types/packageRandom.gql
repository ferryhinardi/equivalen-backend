type PackageRandom {
  id: ID!
  orderNo: Int
  user: User
  package: Package
  question: Question
  userAnswer: UserAnswer
}

extend type Mutation {
  generateRandomQuestion(archiveId: ID): [PackageRandom]
}
