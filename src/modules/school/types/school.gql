type School {
  id: ID!
  name: String!
  city: String
  province: String
  district: String
}

input SchoolInput {
  name: String!
  city: String
  province: String
  district: String
}

extend type Query {
  """
  Get list of schools.
  """
  schools(keyword: String): [School!]
}
